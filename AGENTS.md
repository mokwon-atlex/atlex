# Atlex AI 작업 지침

이 파일은 AI 작업자가 Atlex 저장소에서 작업할 때 따라야 하는 진입점입니다. 세부 규칙은 아래 문서를 원본으로 사용합니다.

## 필수 문서

작업을 시작하기 전에 다음 문서를 확인합니다.

1. 현재 사용자의 지시와 연결된 GitHub 이슈
2. 이슈에 연결된 Notion 문서
3. `docs/ai/README.md`
4. `CONTRIBUTING.md`
5. 변경 경로에 따른 영역별 문서
   - `frontend/**`: `docs/ai/frontend.md`
   - `backend/**`: `docs/ai/backend.md`

프론트엔드와 백엔드를 함께 변경하면 두 영역별 문서를 모두 확인합니다.

## 규칙 우선순위

현재 사용자의 명시적 지시, GitHub 이슈, 연결된 Notion 문서, AI 협업 문서, 기여 가이드, 기존 코드 패턴 순으로 적용합니다. 서로 충돌하거나 요구사항이 불명확하면 임의로 선택하지 않고 사용자에게 확인합니다.

## 기본 원칙

- 작업 전 `git status`로 기존 변경을 확인합니다.
- `git status`, 브랜치 확인과 정기 검증 같은 기본 절차는 조용히 수행하고, 문제나 사용자 판단이 필요할 때만 알립니다.
- 현재 이슈의 범위만 변경하고 범위 밖 문제는 별도 이슈로 제안합니다.
- 로컬 수정과 검증은 자율적으로 수행하되 커밋은 사용자가 요청한 경우에만 생성합니다.
- push, PR 생성과 GitHub 외부 상태 변경은 실행 직전에 사용자 승인을 받습니다.
- PR 승인과 병합은 사용자가 직접 수행합니다.
- 코드 주석, 저장소 문서와 작업 보고는 한국어로 작성합니다.
- AI 협업 문서는 현재 작업에 필요한 범위에서만 작게 갱신합니다.
