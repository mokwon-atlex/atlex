# Atlex 기여 가이드

이 문서는 Atlex 저장소의 개발, 검증, 리뷰 기준을 정의합니다. 프론트엔드와 백엔드는 하나의 기능을 완성하는 단위로 함께 개발하며, 각 영역의 내부 구현은 명확한 경계를 통해 격리합니다.

## 1. 기본 원칙

- 하나의 이슈는 하나의 PR로 완료합니다.
- 작업 규모가 크다면 독립적으로 완료할 수 있는 이슈로 먼저 나눕니다.
- 하나의 기능에 필요한 프론트엔드, 백엔드, API 명세, 테스트를 같은 PR에서 함께 변경합니다.
- 작업 범위와 관계없는 리팩터링이나 정리를 같은 PR에 섞지 않습니다.
- 단순함을 우선하며 실제 요구가 없는 추상화와 확장 지점을 미리 만들지 않습니다.
- 코드 길이가 아니라 책임, 반복, 변경 이유와 이해 가능성을 기준으로 코드를 분리합니다.

## 2. Git과 GitHub

### 작업 타입

| 타입       | 용도                            |
| ---------- | ------------------------------- |
| `feat`     | 새로운 기능 또는 기존 기능 확장 |
| `fix`      | 버그 수정                       |
| `refactor` | 동작을 유지하는 코드 구조 개선  |
| `test`     | 테스트 추가 또는 수정           |
| `docs`     | 문서 추가 또는 수정             |
| `ci`       | CI·CD와 자동화 변경             |
| `chore`    | 설정, 의존성과 기타 유지보수    |

### 제목과 브랜치

- 이슈 제목: `type: 작업 요약`
- PR 제목: `type: 작업 요약`
- 커밋 제목: `type: 작업 요약`
- 브랜치 이름: `<type>/<이슈번호>-<작업요약>`
- 이슈, PR과 커밋의 제목은 핵심 내용이 드러나는 간결한 한국어 단답식으로 작성합니다.
- 이슈와 PR 본문, 필요한 경우의 커밋 본문도 소제목과 짧은 단답식 항목을 사용합니다.
- `추가했습니다`, `수정합니다`와 같은 문장형 표현보다 `추가`, `수정`과 같은 명사형 표현을 사용합니다.
- 브랜치 이름의 `<작업요약>`만 짧은 영문 `kebab-case`로 작성합니다.

```text
feat: 소셜 로그인 추가
feat/123-social-login
```

```text
권장: feat: 소셜 로그인 추가
지양: feat: 소셜 로그인 기능을 추가했습니다
```

### 이슈와 PR 연결

- 이슈를 완료하는 PR은 본문에 `Closes #123`을 작성합니다.
- 불가피하게 하나의 이슈를 여러 PR로 나누면 중간 PR에 `Related to #123`을 작성합니다.
- 최종 PR에만 `Closes`를 사용합니다.
- Squash merge 커밋은 PR 제목을 제목으로, PR 본문을 커밋 본문으로 사용합니다.

### 커밋 분리

- 서로 다른 관심사는 별도 커밋으로 분리합니다.
- 기능, 버그 수정, 리팩터링과 테스트 변경을 하나의 커밋에 섞지 않습니다.
- 포맷만 변경한 커밋에는 기능 변경을 포함하지 않습니다.
- 작업 규모가 크거나 변경 내용이 복잡해 제목만으로 목적과 영향을 설명하기 어렵다면 커밋 본문을 작성합니다.
- 커밋 본문에는 변경 이유, 핵심 내용, 영향과 주의사항 중 필요한 항목을 한국어 단답식으로 간결하게 작성합니다.
- 제목만으로 충분한 단순 변경에는 본문을 형식적으로 반복하지 않습니다.

## 3. 공통 코드 작성 규칙

### 네이밍

- 코드 식별자는 영어로 작성합니다.
- 클래스와 React 컴포넌트는 `PascalCase`를 사용합니다.
- 변수, 함수와 메서드는 `camelCase`를 사용합니다.
- 상수는 `UPPER_SNAKE_CASE`를 사용합니다.
- 함수와 메서드 이름은 동사로 시작합니다.
- boolean 이름은 `is`, `has`, `can`, `should`처럼 상태를 나타내는 접두사로 시작합니다.
- 약어는 일반 단어처럼 `userId`, `apiUrl`, `httpClient`로 작성합니다.
- `data`, `info`, `temp`, `util`처럼 범위가 넓은 이름 대신 역할을 드러내는 이름을 사용합니다.
- 같은 도메인 개념은 프론트엔드와 백엔드에서 가능한 한 같은 영문 용어를 사용합니다.
- 반복되는 숫자와 문자열은 의미가 드러나는 상수로 분리합니다.

### 책임과 분리

- 실제로 반복되는 구간이 있거나 하나의 메서드가 여러 책임을 가질 때 코드를 분리합니다.
- 조건 분기와 중첩 때문에 핵심 흐름이 읽기 어렵다면 의미 단위로 분리합니다.
- 한 번만 사용되는 단순 코드를 길이만으로 작은 함수에 나누지 않습니다.
- 공통화로 도메인 의미가 흐려진다면 억지로 합치지 않고 제한적인 중복을 허용합니다.
- 분리한 클래스와 메서드는 이름만으로 책임을 이해할 수 있어야 합니다.
- 단순 위임만 하는 Service, 인터페이스와 Wrapper를 만들지 않습니다.

### SOLID

- 하나의 클래스와 메서드는 하나의 주요 책임과 변경 이유를 갖습니다.
- 실제 확장 요구가 있을 때 기존 동작을 보호하는 확장 지점을 만듭니다.
- 하위 타입은 상위 타입의 계약과 기대 동작을 깨지 않습니다.
- 사용하지 않는 기능까지 의존하게 만드는 큰 인터페이스를 피합니다.
- 외부 시스템이나 변경 가능성이 큰 구현은 명확한 경계 뒤에 배치합니다.

### 변경 영향 격리

- 각 영역은 다른 영역의 내부 구현이 아닌 공개된 계약을 통해 통신합니다.
- 경계를 통과하는 데이터는 DTO 또는 명확한 입력·출력 모델로 전달합니다.
- 구현 변경이 호출자에게 전파되지 않도록 인터페이스와 Adapter를 활용합니다.
- 도메인과 모듈 사이의 의존 방향은 단방향으로 유지하며 순환 의존을 만들지 않습니다.
- 인터페이스는 외부 시스템, 도메인 경계, 교체 가능한 정책과 테스트 격리가 필요한 지점에 사용합니다.
- 모든 Service에 형식적으로 인터페이스를 만들지 않습니다.
- 계약이 변경되면 소비자, 테스트와 API 명세를 같은 PR에서 수정합니다.

## 4. 프론트엔드

### 디렉터리 책임

- `app`: 라우팅, 페이지, 레이아웃과 Provider 구성
- `components/common`: 도메인에 종속되지 않는 공통 UI
- `components/domain/<도메인>`: 도메인별 화면과 기능
- `hooks`: 재사용 가능한 상태와 데이터 Hook
- `lib/api`: HTTP 요청과 응답 처리
- `lib`: 순수 함수와 공통 로직
- `store`: 전역 클라이언트 상태
- `data`: 정적 데이터와 목 데이터
- `stories`: Storybook 스토리

`common`은 특정 `domain` 모듈을 참조하지 않습니다. 도메인 코드가 공통 컴포넌트와 모듈을 조합합니다.

### 파일과 컴포넌트

- React 컴포넌트 파일은 `PascalCase.jsx`로 작성합니다.
- JSX가 없는 모듈 파일은 역할을 나타내는 `kebab-case.js`로 작성합니다.
- Hook은 `use`로 시작하는 `camelCase`를 사용합니다.
- API 함수는 `loginApi`, `createPostApi`처럼 동작과 대상을 함께 표현합니다.
- 컴포넌트는 named export를 기본으로 사용합니다.
- Next.js가 요구하는 `page`, `layout` 등은 default export를 허용합니다.
- JSX가 있으면 `.jsx`, 없으면 `.js`를 사용합니다.
- 파일 하나는 하나의 주요 책임을 담당합니다.
- 작은 전용 하위 컴포넌트는 같은 파일에 둘 수 있습니다.

### 구현

- 내부 모듈은 `@/` 별칭으로 가져옵니다.
- import는 외부 패키지, 내부 공통 모듈, 도메인 모듈 순서로 구분합니다.
- Server Component를 기본으로 하고 Hook, 브라우저 API와 이벤트 처리가 필요한 파일에만 `'use client'`를 사용합니다.
- 지역 상태는 React 상태, 서버 상태는 TanStack Query, 전역 클라이언트 상태는 Zustand로 관리합니다.
- 컴포넌트 props, Custom Hook의 입력·출력, API 모듈과 Store의 공개 상태를 인터페이스로 취급합니다.
- 반복되거나 복잡한 객체 계약은 JSDoc의 `@typedef`로 명시합니다.
- Tailwind CSS를 기본으로 사용하고 조건부 클래스는 `cn`, 컴포넌트 변형은 `cva`로 관리합니다.
- 동적으로 계산해야 하는 스타일을 제외하고 인라인 스타일을 사용하지 않습니다.

### 접근성

- 클릭 동작에는 `button`, 이동에는 `a`처럼 의미에 맞는 HTML 요소를 사용합니다.
- 키보드 조작과 focus 상태를 지원합니다.
- 입력 요소에는 연결된 label을 제공합니다.
- 의미가 있는 이미지에는 적절한 대체 텍스트를 제공합니다.

## 5. 백엔드

### 패키지와 계층

- `domain/<도메인>` 아래에 `controller`, `service`, `repository`, `entity`, `dto`, `exception`을 배치합니다.
- `global`에는 보안, 공통 응답과 전역 예외처럼 여러 도메인이 공유하는 코드만 배치합니다.
- Controller는 요청 검증, 인증 정보 전달과 응답 변환만 담당합니다.
- Service는 유스케이스와 트랜잭션 경계를 담당합니다.
- Entity는 자신의 상태 변경과 불변 조건을 담당합니다.
- Repository는 데이터 조회와 저장만 담당합니다.
- Entity를 API 요청이나 응답으로 직접 노출하지 않습니다.

### 구현

- 의존성은 생성자 주입을 사용하며 `@RequiredArgsConstructor`를 허용합니다.
- 조회 메서드는 `@Transactional(readOnly = true)`, 변경 메서드는 `@Transactional`을 사용합니다.
- 입력 형식은 Bean Validation으로 검증하고 도메인 규칙은 Service 또는 Entity에서 검증합니다.
- Entity에는 공개 setter를 두지 않고 의미가 드러나는 상태 변경 메서드를 사용합니다.
- Entity의 기본 생성자는 `PROTECTED`로 제한합니다.
- JPA 연관관계는 기본적으로 지연 로딩을 사용합니다.
- Repository의 단건 조회 반환값에는 `Optional`을 허용합니다.
- 필드, 매개변수와 DTO 속성에는 `Optional`을 사용하지 않습니다.
- 와일드카드 import를 사용하지 않습니다.
- Lombok `@Data`를 지양하고 필요한 어노테이션만 선택합니다.
- 예상 가능한 실패는 도메인 예외로 표현하고 전역 예외 처리기에서 API 응답으로 변환합니다.

## 6. 주석과 문서

- 코드 주석과 저장소 문서는 한국어로 작성합니다.
- 코드 식별자와 기술 고유명사는 원문을 유지합니다.
- 주석은 코드 내용을 그대로 읽어주기보다 존재 이유, 처리 흐름, 제약과 예외 상황을 설명합니다.
- 코드 변경으로 주석이 틀려지면 같은 PR에서 수정하거나 삭제합니다.
- `TODO`에는 관련 이슈 번호와 제거 조건을 함께 작성합니다.

### Java

- 클래스, 메서드와 필드에 한국어 Javadoc을 작성합니다.
- 매개변수, 반환값과 예외 의미가 필요하면 `@param`, `@return`, `@throws`를 작성합니다.
- Swagger `@Schema`가 있어도 코드 문서용 Javadoc을 별도로 작성합니다.
- 복잡한 메서드 내부에는 처리 흐름을 설명하는 한국어 한 줄 주석을 작성합니다.

### JavaScript와 React

- 모든 컴포넌트, Hook과 함수에 한국어 JSDoc을 작성합니다.
- 모듈 상수와 의미 있는 상태·데이터 변수에 한국어 설명을 작성합니다.
- props와 반환값의 의미가 분명하지 않다면 `@param`, `@returns`를 작성합니다.
- 비동기 처리, 상태 전환, 예외 복구와 데이터 변환 순서를 한 줄 주석으로 설명합니다.
- 조건문의 업무적 이유와 일반적이지 않은 라이브러리 사용 이유를 기록합니다.
- 단순 반복문 인덱스, 구조 분해 변수와 즉시 이해되는 이벤트 매개변수에는 개별 주석을 생략할 수 있습니다.
- 상위 JSDoc에서 충분히 설명한 내용을 내부에서 반복하지 않습니다.

## 7. 테스트

### 공통 원칙

- 기능 추가와 버그 수정에는 관련 테스트를 함께 작성합니다.
- 리팩터링은 기존 테스트가 통과하는 상태에서 진행합니다.
- 테스트 하나는 하나의 동작 또는 실패 원인만 검증합니다.
- 구현 세부사항보다 외부에서 관찰할 수 있는 동작을 검증합니다.
- Given–When–Then 구조를 사용합니다.
- 시간, 네트워크와 실행 순서에 따라 결과가 달라지는 테스트를 만들지 않습니다.
- 버그 수정에는 같은 문제가 다시 발생하지 않도록 회귀 테스트를 추가합니다.
- 의미 없는 100% 커버리지보다 핵심 동작과 경계 조건 검증을 우선합니다.

### 프론트엔드

- Vitest와 Testing Library를 기본으로 사용합니다.
- 사용자 입력, 화면 변화와 API 요청 결과를 사용자 관점에서 검증합니다.
- 요소는 role, label, text 등 접근성 기반 쿼리로 찾습니다.
- DOM 구조나 CSS 클래스 같은 구현 세부사항을 과도하게 검증하지 않습니다.
- API 경계는 모킹할 수 있지만 내부 함수와 Hook을 지나치게 모킹하지 않습니다.
- 공통 UI와 주요 화면 상태는 Storybook 스토리로 작성합니다.
- Playwright E2E는 핵심 사용자 흐름에만 사용합니다.

### 백엔드

- Entity와 Service의 핵심 규칙은 단위 테스트로 검증합니다.
- Controller 요청·응답 계약은 MVC 테스트로 검증합니다.
- Repository 쿼리는 실제 데이터베이스 동작이 필요한 슬라이스 테스트로 검증합니다.
- 전체 Spring Context가 필요한 경우에만 통합 테스트를 사용합니다.
- 정상 흐름과 함께 권한, 검증 실패, 중복과 존재하지 않는 자원 등 예외 흐름을 검증합니다.
- 테스트 메서드 이름은 영어 식별자를 사용하고 `@DisplayName`은 한국어로 작성합니다.
- 외부 API와 시간 의존성은 테스트에서 제어할 수 있는 경계로 분리합니다.

## 8. 예외와 로그

### 예외 처리

- Controller에서 개별 `try-catch`로 응답을 만들지 않고 전역 예외 처리기를 사용합니다.
- 예상 가능한 실패는 목적이 명확한 도메인 예외로 정의합니다.
- 하나의 예외를 여러 의미로 재사용하지 않습니다.
- 예외를 잡아 무시하지 않으며 다른 예외로 변환할 때 원인을 보존합니다.
- 클라이언트 오류와 서버 오류를 HTTP 상태 코드로 구분합니다.
- 사용자에게 내부 클래스명, SQL과 스택 트레이스 같은 구현 정보를 노출하지 않습니다.
- 오류 응답은 공통 API 응답 형식을 유지합니다.
- 프론트엔드가 분기 기준으로 사용하는 오류 코드는 안정적으로 관리합니다.

### 로그

- `System.out`, `System.err`와 `printStackTrace`를 사용하지 않습니다.
- SLF4J 로거와 `{}` 자리표시자를 사용합니다.
- 정상적인 주요 상태 변화는 `info`, 복구 가능하거나 확인이 필요한 상황은 `warn`으로 기록합니다.
- 요청을 처리할 수 없는 예상 밖 오류는 `error`, 개발용 세부 정보는 `debug`로 기록합니다.
- 비밀번호, 토큰, 인증 헤더와 개인정보를 로그에 기록하지 않습니다.
- 로그 메시지는 한국어로 작성하고 식별자와 상태명 같은 기술 값은 원문을 유지합니다.

## 9. API와 데이터

- API 경로는 `/api/v1` 버전을 유지합니다.
- URL은 소문자 명사와 복수형을 기본으로 하며 행위보다 자원을 중심으로 설계합니다.
- JSON 필드명은 `camelCase`를 사용합니다.
- 날짜와 시간은 ISO 8601 형식으로 전송하며 시간대 기준을 명시합니다.
- 요청 DTO와 응답 DTO를 분리합니다.
- 목록 조회의 페이지와 커서 형식을 기능마다 임의로 만들지 않고 통일합니다.
- HTTP 상태 코드는 응답 의미에 맞게 사용합니다.
- API나 데이터 구조를 변경하면 프론트엔드, 백엔드, Swagger와 테스트를 같은 PR에서 수정합니다.
- 기존 계약을 깨는 필드 제거와 타입 변경은 영향 범위와 마이그레이션 방법을 PR에 명시합니다.
- 가능하면 이전 프론트엔드와 새 백엔드가 함께 동작할 수 있도록 하위 호환성을 유지합니다.
- 가능한 값은 불변으로 관리하고 `null` 사용 범위를 최소화합니다.
- 사용자 입력은 시스템 경계에서 검증하고 필요한 경우 정규화합니다.

## 10. 보안과 의존성

- 비밀 값과 개인정보를 코드, 로그, 이슈와 PR에 기록하지 않습니다.
- 실제 환경 변수는 커밋하지 않고 `frontend/.env.example`, `backend/.env.example`만 공유합니다.
- 프론트엔드와 백엔드는 각각 자신의 `.env`를 사용합니다.
- 권한 검사는 프론트엔드 화면에 의존하지 않고 백엔드에서 최종 수행합니다.
- 새 라이브러리는 기존 도구로 해결하기 어려운 경우에만 추가합니다.
- 라이브러리 추가 이유와 영향 범위를 PR에 작성합니다.
- 같은 역할의 라이브러리를 중복해서 도입하지 않습니다.

## 11. 파일과 환경 호환성

- UTF-8 인코딩과 LF 줄바꿈을 사용합니다.
- import 경로의 대소문자를 실제 파일명과 정확히 일치시킵니다.
- 운영체제에 종속된 경로 구분자와 셸 문법을 프로젝트 명령에 사용하지 않습니다.
- 사용하지 않는 코드와 주석 처리된 코드는 삭제합니다.
- 임시 구현에는 관련 이슈 번호와 제거 조건을 기록합니다.

## 12. 포맷과 검증

프론트엔드:

```bash
cd frontend
npm run format:check
npm test
npm run build
```

프론트엔드 전체 포맷 적용:

```bash
cd frontend
npm run format
```

백엔드 macOS/Linux:

```bash
cd backend
./gradlew spotlessCheck
./gradlew test
./gradlew bootJar
```

백엔드 Windows PowerShell:

```powershell
cd backend
.\gradlew.bat spotlessCheck
.\gradlew.bat test
.\gradlew.bat bootJar
```

백엔드 전체 포맷 적용:

```bash
cd backend
./gradlew spotlessApply
```

PR을 생성하기 전에는 변경 범위와 관계없이 저장소 루트에서 `npm run check`를 실행하여 프론트엔드와 백엔드의 포맷, 테스트와 빌드를 모두 통과해야 합니다.

## 13. 코드 리뷰

- 모든 PR은 Draft로 생성합니다.
- Draft 상태에서 CodeRabbit 리뷰를 완료하고 모든 리뷰 의견을 검토·처리합니다.
- 전체 검사와 CI가 통과하고 모든 리뷰 대화가 해결되면 사용자가 직접 Ready for review로 전환합니다.
- Ready for review 전환 시 PR 작성자를 제외한 저장소의 모든 사용자 협업자에게 리뷰가 자동 요청됩니다.
- 모든 팀원이 PR을 검토하는 것을 원칙으로 합니다.
- `develop`과 `main` 대상 PR은 최소 2명의 승인을 받습니다.
- 특정 팀원을 필수 리뷰어로 지정하지 않습니다.
- PR 제목과 본문이 작업 목적과 변경 범위를 정확히 설명하는지 확인합니다.
- 이슈의 완료 조건이 코드와 테스트로 충족되는지 확인합니다.
- 변경이 다른 도메인과 모듈의 내부 구현에 불필요하게 의존하지 않는지 확인합니다.
- API와 데이터 계약 변경이 소비자, Swagger와 테스트에 반영됐는지 확인합니다.
- 보안, 권한, 예외 처리와 로그에 민감 정보 노출 위험이 없는지 확인합니다.
- 새로운 추상화와 의존성이 실제 필요에 의해 추가됐는지 확인합니다.
- 주석과 문서가 현재 코드와 일치하며 한국어로 작성됐는지 확인합니다.
- 프론트엔드와 백엔드의 필수 검증 결과를 확인합니다.
- 리뷰 대화가 모두 해결된 뒤 병합합니다.
